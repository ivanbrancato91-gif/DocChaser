'use client';import {useState} from 'react';export default function Correction(){const [sent,setSent]=useState(false);return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20}}><div className="card" style={{maxWidth:560,width:'100%'}}><h1>Documento da correggere</h1><p className="muted">La carta d’identità non è sufficientemente leggibile.</p><textarea style={{width:'100%',minHeight:110,padding:12,border:'1px solid #e4eaf2',borderRadius:10}} placeholder="Messaggio al cliente"/><button className="btn primary" style={{marginTop:14}} onClick={()=>setSent(true)}>Invia richiesta</button>{sent&&<p style={{color:'#118052',fontWeight:700}}>Richiesta inviata.</p>}</div></main>}
