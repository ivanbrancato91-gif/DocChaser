'use client';import {useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';export default function Login(){const r=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async()=>{setBusy(true);setError('');const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await res.json();if(!res.ok)setError(d.error||'Accesso non riuscito');else { const next = new URLSearchParams(window.location.search).get('next'); r.push(next?.startsWith('/')?next:'/dashboard'); }setBusy(false)};return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20}}><div className="card" style={{width:'100%',maxWidth:430}}><div className="brand" style={{padding:0}}>Doc<span>Chaser</span></div><h1>Accedi</h1><input className="search" style={{width:'100%',marginBottom:10}} placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="search" style={{width:'100%'}} placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn primary" style={{width:'100%',justifyContent:'center',marginTop:16}} disabled={busy} onClick={submit}>{busy?'Accesso...':'Accedi'}</button>{error&&<p style={{color:'#b42318'}}>{error}</p>}<p className="muted" style={{fontSize:13}}><Link href="/recover">Password dimenticata?</Link> · <Link href="/register">Registrati</Link></p></div></main>}
