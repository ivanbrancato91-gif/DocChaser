'use client';import {useState} from 'react';export default function Recover(){const [email,setEmail]=useState('');const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const submit=async()=>{setBusy(true);setError('');const r=await fetch('/api/auth/recover',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();if(!r.ok)setError(d.error||'Errore');else setSent(true);setBusy(false)};return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20}}><div className='card' style={{width:'100%',maxWidth:430}}><div className='brand' style={{padding:0}}>Doc<span>Chaser</span></div><h1>Recupera password</h1><p className='muted'>Inserisci l'email del tuo account. Se esiste, riceverai un link sicuro.</p><input className='search' style={{width:'100%'}} placeholder='Email' type='email' value={email} onChange={e=>setEmail(e.target.value)}/><button className='btn primary' style={{marginTop:16}} disabled={busy||!email} onClick={submit}>{busy?'Invio…':'Invia istruzioni'}</button>{sent&&<p style={{color:'#118052',fontWeight:700}}>Se l'indirizzo esiste, riceverai le istruzioni per reimpostare la password.</p>}{error&&<p style={{color:'#b42318'}}>{error}</p>}</div></main>}