import Link from 'next/link';
export default function Page({title,description,action,children}:{title:string;description?:string;action?:{label:string;href:string};children?:React.ReactNode}){return <div className="content"><div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start',gap:20,marginBottom:22}}><div><div className="h1">{title}</div>{description&&<div className="muted" style={{marginTop:7}}>{description}</div>}</div>{action&&<Link className="btn primary" href={action.href}>{action.label}</Link>}</div>{children||<div className="card"><p className="muted">Area pronta per la gestione di {title.toLowerCase()}.</p></div>}</div>}
